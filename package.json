{
  "name": "railsmart",
  "version": "1.0.0",
  "description": "RailSmart - AI-Powered Smart Train Booking and Railway Travel Management System",
  "main": "server/server.js",
  "scripts": {
    "server": "node server/server.js",
    "server:dev": "nodemon server/server.js",
    "client": "cd client && npm run dev",
    "dev": "concurrently \"npm run server:dev\" \"npm run client\"",
    "seed": "node server/seed.js",
    "install-deps": "npm install --prefix server && npm install --prefix client",
    "build": "npm run install-deps && cd client && npm run build",
    "vercel-build": "npm run install-deps && cd client && npm run build",
    "start": "node server/server.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.6",
    "dotenv": "^16.6.1",
    "express": "^4.22.2",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^8.24.4",
    "socket.io": "^4.8.3"
  },
  "devDependencies": {
    "concurrently": "^10.0.5",
    "nodemon": "^3.1.14"
  }
}
